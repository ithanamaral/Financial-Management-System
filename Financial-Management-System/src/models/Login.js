class Login {

    constructor(email, password) {
        this.email = email;
        this.password = password;
    }

    authenticate() {    
    }

    validateEmail(email) {
     
    }

    validatePassword(password) {
        
    }
}

module.exports = Login;